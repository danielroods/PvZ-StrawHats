# Copy this file to server-data/ta.properties if you want real email delivery for the demo.
# Keep smtp.password out of source control.
smtp.enabled=false
smtp.host=smtp.example.com
smtp.port=465
smtp.username=your-smtp-account@example.com
smtp.password=your-smtp-password
smtp.from=your-smtp-account@example.com
